# This is a configuration definition file in folder "/home/jim/LTLMoP/src/examples/projectPR3/configs".
# Format details are described at the beginning of each section below.


======== General Config ========

Actuator_Proposition_Mapping: # Mapping between actuator propositions and actuator handler functions
Return_Letter = MAE.naoActuator.runBehavior(startBehaviorName="returnPackage",endBehaviorName="",repeat=False,repeat_period=)
isOpen = share.dummyActuator.setActuator(name="isOpen")
resynthesize = share.dummyActuator.setActuator(name="resynthesize")
lol = share.dummyActuator.setActuator(name="lol")
IDLE_MODE = share.dummyActuator.setActuator(name="IDLE_MODE")
Deliver_Letter = MAE.naoActuator.runBehavior(startBehaviorName="deliver",endBehaviorName="",repeat=False,repeat_period=)
Knock = MAE.naoActuator.runBehavior(startBehaviorName="knockDoor",endBehaviorName="",repeat=False,repeat_period=)
EXPLORE_SETUP_MODE = share.dummyActuator.setActuator(name="EXPLORE_SETUP_MODE")
requestExplore = Segway.CSharpRobotActuator.requestExplore()
isClosed = share.dummyActuator.setActuator(name="isClosed")
Pickup_Letter = MAE.naoActuator.receivePackage(landmarkSize=0.04,camera=1)
RESYNTHESIZE_MODE = share.dummyActuator.setActuator(name="RESYNTHESIZE_MODE")
knock = share.dummyActuator.setActuator(name="knock")
EXPLORE_MODE = share.dummyActuator.setActuator(name="EXPLORE_MODE")

Initial_Truths: # Initially true propositions
explore
IDLE_MODE
isOpen

Main_Robot: # The name of the robot used for moving in this config
Segway

Name: # Configuration name
ProjectPR3_upson

Region_Tags: # Mapping from tag names to region groups, for quantification
{}

Sensor_Proposition_Mapping: # Mapping between sensor propositions and sensor handler functions
Letter_Classroom = share.dummySensor.buttonPress(button_name="Letter_Classroom",init_value=False)
Door_Closed = Segway.CSharpRobotSensor.doorClosed()
BUSY_EXPLORING = Segway.CSharpRobotSensor.busyExploring()
ARTAG103 = Segway.CSharpRobotSensor.artag_detection(ARTagID=103)
region_added = Segway.CSharpRobotSensor.regionAdded()
open_doorway = Segway.CSharpRobotSensor.open_doorway()
initLIDAR = Segway.CSharpRobotSensor.init_lidar()
exploreHack = Segway.CSharpRobotSensor.open_doorway()
Letter_Campbell = share.dummySensor.buttonPress(button_name="Letter_Campbell",init_value=False)
init_lidar_Spyros = Segway.CSharpRobotSensor.init_lidar()
door_closed = Segway.CSharpRobotSensor.doorClosed()
ARTAG64 = Segway.CSharpRobotSensor.artag_detection(ARTagID=64)
LIDAR = Segway.CSharpRobotSensor.lidar_detection()
Person = MAE.naoSensor.seePerson(hold_time=2.0)
ARTAG = Segway.CSharpRobotSensor.artag_detection(ARTagID=64)
NEW_DOORWAY = Segway.CSharpRobotSensor.open_doorway()
Letter_KressGazit = MAE.naoSensor.seeLandMark(landMark_id=187)
region_removed = Segway.CSharpRobotSensor.regionRemoved()


======== Robot1 Config ========

ActuatorHandler: # Actuator handler file in robots/Type folder
CSharpRobotActuator()

CalibrationMatrix: # 3x3 matrix for converting coordinates, stored as lab->map
array([[  3.65181000e+01,   4.55120000e-01,   1.14010000e+03],
       [  0.00000000e+00,  -3.50429000e+01,   3.75500000e+02],
       [  0.00000000e+00,   0.00000000e+00,   1.00000000e+00]])

DriveHandler: # Input value for robot drive handler, refer to file inside the handlers/drive folder
differentialDrive(d=0.3)

InitHandler: # Input value for robot init handler, refer to the init file inside the handlers/robots/Type folder
CSharpRobotInit(robotType=2,IPAddress="192.168.1.91",commPort=7400,buffer=2048576)

LocomotionCommandHandler: # Input value for robot locomotion command handler, refer to file inside the handlers/robots/Type folder
CSharpRobotLocomotionCommand()

MotionControlHandler: # Input value for robot motion control handler, refer to file inside the handlers/motionControl folder
RRTController(robot_type=5,max_angle_goal=3.14,max_angle_overlap=3.14,plotting=True,do_pathfollowing=False,target_threshold=1.5)

PoseHandler: # Input value for robot pose handler, refer to file inside the handlers/pose folder
CSharpPose(x=0.0,y=0.0,yaw=0.0)

RobotName: # Robot Name
Segway

SensorHandler: # Sensor handler file in robots/Type folder
CSharpRobotSensor()

Type: # Robot type
CSharpRobot


======== Robot2 Config ========

ActuatorHandler: # Actuator handler file in robots/Type folder
naoActuator()

DriveHandler: # Input value for robot drive handler, refer to file inside the handlers/drive folder
bipedalDrive(maxspeed=1.0,maxfreq=1.0,angcur=1.047,angfwd=0.262,minvel=0.3,silent=True)

InitHandler: # Input value for robot init handler, refer to the init file inside the handlers/robots/Type folder
naoInit(ip="maeby.local",port=9559)

LocomotionCommandHandler: # Input value for robot locomotion command handler, refer to file inside the handlers/robots/Type folder
naoLocomotionCommand()

MotionControlHandler: # Input value for robot motion control handler, refer to file inside the handlers/motionControl folder
vectorController()

PoseHandler: # Input value for robot pose handler, refer to file inside the handlers/pose folder
viconPose(host="10.0.0.102",port=800,x_VICON_name="Nao:Nao <t-X>",y_VICON_name="Nao:Nao <t-Y>",theta_VICON_name="Nao:Nao <a-Z>")

RobotName: # Robot Name
MAE

SensorHandler: # Sensor handler file in robots/Type folder
naoSensor()

Type: # Robot type
nao

